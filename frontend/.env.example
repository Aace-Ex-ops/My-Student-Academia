# Local Development (Default - uses Vite proxy to http://localhost:5000):
VITE_API_BASE_URL=/api

# Production Cloud Deployment:
# Set this to your deployed backend URL (e.g. Google Cloud Run, Render, Railway, Fly.io):
# VITE_API_BASE_URL=https://my-student-academia-api.onrender.com/api
