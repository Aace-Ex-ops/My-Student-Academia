version: '3.8'

services:
  app:
    build: .
    ports:
      - "5000:5000"
      - "5173:5173"
    environment:
      - PORT=5000
      - NODE_ENV=production
    volumes:
      - ./backend/prisma/dev.db:/app/backend/prisma/dev.db
